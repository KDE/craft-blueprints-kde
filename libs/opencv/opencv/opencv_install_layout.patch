--- a/cmake/OpenCVInstallLayout.cmake
+++ b/cmake/OpenCVInstallLayout.cmake
@@ -37,13 +37,13 @@
     set(_jni_suffix "")
   endif()
 
-  ocv_update(OPENCV_BIN_INSTALL_PATH           "${OPENCV_INSTALL_BINARIES_PREFIX}bin")
-  ocv_update(OPENCV_TEST_INSTALL_PATH          "${OPENCV_BIN_INSTALL_PATH}")
-  ocv_update(OPENCV_SAMPLES_BIN_INSTALL_PATH   "${OPENCV_INSTALL_BINARIES_PREFIX}samples")
-  ocv_update(OPENCV_LIB_INSTALL_PATH           "${OPENCV_INSTALL_BINARIES_PREFIX}${OPENCV_INSTALL_BINARIES_SUFFIX}")
-  ocv_update(OPENCV_LIB_ARCHIVE_INSTALL_PATH   "${OPENCV_LIB_INSTALL_PATH}")
-  ocv_update(OPENCV_3P_LIB_INSTALL_PATH        "${OPENCV_INSTALL_BINARIES_PREFIX}staticlib")
-  ocv_update(OPENCV_CONFIG_INSTALL_PATH        ".")
+  ocv_update(OPENCV_BIN_INSTALL_PATH           "bin")
+  ocv_update(OPENCV_TEST_INSTALL_PATH          "bin")
+  ocv_update(OPENCV_SAMPLES_BIN_INSTALL_PATH   "samples")
+  ocv_update(OPENCV_LIB_INSTALL_PATH           "${OPENCV_INSTALL_BINARIES_SUFFIX}")
+  ocv_update(OPENCV_LIB_ARCHIVE_INSTALL_PATH   "${OPENCV_LIB_INSTALL_PATH}")
+  ocv_update(OPENCV_3P_LIB_INSTALL_PATH        "staticlib")
+  ocv_update(OPENCV_CONFIG_INSTALL_PATH        "cmake")
   ocv_update(OPENCV_INCLUDE_INSTALL_PATH       "include")
   ocv_update(OPENCV_OTHER_INSTALL_PATH         "etc")
   ocv_update(OPENCV_SAMPLES_SRC_INSTALL_PATH   "samples")
--- a/cmake/templates/OpenCVConfig.cmake.in
+++ b/cmake/templates/OpenCVConfig.cmake.in
@@ -142,14 +142,14 @@
 endif()
 
 
-if(NOT DEFINED OPENCV_MAP_IMPORTED_CONFIG)
-  if(CMAKE_GENERATOR MATCHES "Visual Studio" OR MSVC)
-    # OpenCV supports Debug and Release builds only.
-    # But MSVS has 'RelWithDebInfo' 'MinSizeRel' configurations for applications.
-    # By default CMake maps these configuration on the first available (Debug) which is wrong.
-    # Non-Debug build of Application can't be used with OpenCV Debug build (ABI mismatch problem)
-    # Add mapping of RelWithDebInfo and MinSizeRel to Release here
-    set(OPENCV_MAP_IMPORTED_CONFIG "RELWITHDEBINFO=!Release;MINSIZEREL=!Release")
-  endif()
-endif()
+#if(NOT DEFINED OPENCV_MAP_IMPORTED_CONFIG)
+#  if(CMAKE_GENERATOR MATCHES "Visual Studio" OR MSVC)
+#    # OpenCV supports Debug and Release builds only.
+#    # But MSVS has 'RelWithDebInfo' 'MinSizeRel' configurations for applications.
+#    # By default CMake maps these configuration on the first available (Debug) which is wrong.
+#    # Non-Debug build of Application can't be used with OpenCV Debug build (ABI mismatch problem)
+#    # Add mapping of RelWithDebInfo and MinSizeRel to Release here
+#    set(OPENCV_MAP_IMPORTED_CONFIG "RELWITHDEBINFO=!Release;MINSIZEREL=!Release")
+#  endif()
+#endif()
 set(__remap_warnings "")
 macro(ocv_map_imported_config target)
   if(DEFINED OPENCV_MAP_IMPORTED_CONFIG) # list, "RELWITHDEBINFO=Release;MINSIZEREL=Release"
