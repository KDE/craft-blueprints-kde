[General]
name = Qt5
include = version-5.9.ini
tags =
branches = 5.10
# note: 5.6.1-1 tarball won't build using craft (version number inside tarball differs) -- let's just use the branch for now
tarballs = 5.10.0
defaulttarget = 5.9.3
tarballUrl = https://download.qt-project.org/official_releases/qt/${VERSION_MAJOR}.${VERSION_MINOR}/${VERSION}/submodules/${PACKAGE_NAME}-everywhere-src-${VERSION}.tar.xz
tarballDigestUrl = https://download.qt-project.org/official_releases/qt/${VERSION_MAJOR}.${VERSION_MINOR}/${VERSION}/submodules/${PACKAGE_NAME}-everywhere-src-${VERSION}.tar.xz.sha256
tarballInstallSrc = ${PACKAGE_NAME}-everywhere-src-${VERSION}
gitUrl = https://code.qt.io/qt/${PACKAGE_NAME}.git
